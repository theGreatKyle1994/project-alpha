import { useState } from "react";
import { useNavigate } from "react-router-dom";
import axios from "axios";

// Todo for player login/guest bypass
const LoginCore = () => {
  const navigate = useNavigate;
  return (
    <div>
      <h1>Login Page</h1>
      <form></form>
    </div>
  );
};

export default LoginCore;
