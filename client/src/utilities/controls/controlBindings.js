const controlObj = {
  // Default Move Up
  w: false,
  // Default Move Left
  a: false,
  // Default Move Down
  s: false,
  // Default Move Right
  d: false,
  // Default Open Inventory
  tab: false,
};

export default controlObj;
